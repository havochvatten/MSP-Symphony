import { storiesOf } from '@storybook/angular';

import { SliderComponent } from './slider.component';

storiesOf('Base | Slider', module).add('default', () => ({
  component: SliderComponent,
  props: {}
}));
